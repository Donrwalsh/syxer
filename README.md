Get data in raw form, populate mongoDB with it and build actual functional backend to fetch the data.

- having users login is a necessity when we get beyond prototype, so that's another mark in this section.

OR 

Script is smart enough to perform the transformation as part of the fetch action